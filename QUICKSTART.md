# 🚀 Быстрый старт Wizzair Screenshot Monitor

## ✅ ГОТОВО! Проект полностью настроен

Всё работает! Осталось только:
1. **Начать диалог с ботом** в Telegram
2. Запустить Docker

## 🤖 ШАГ 1: Активация бота

**ВАЖНО:** Найдите вашего бота в Telegram и нажмите **Start**

Без этого бот не сможет отправлять вам сообщения!

## 🐳 ШАГ 2: Запуск Docker

```bash
# Запустите Docker Desktop
open -a Docker

# Подождите пока Docker запустится (иконка в трее)

# Запустите проект
cd /Users/semenzidkov/wizz_air_parcer
docker-compose up --build
```

## 📸 Что будет происходить:

Через **15-20 секунд** после запуска:
- Откроется страница Wizzair с рейсом EVN → MIL
- Создастся скриншот страницы
- **Скриншот придёт вам в Telegram!**

Затем **каждые 30 минут** будет приходить новый скриншот с актуальными ценами.

## 📊 Мониторинг

### Просмотр логов:
```bash
docker-compose logs -f
```

### Остановка:
```bash
docker-compose down
```

### Перезапуск:
```bash
docker-compose restart
```

## ⚙️ Текущие селекторы

Сейчас используются селекторы из `.env`:
- Города парсятся ✅
- Дата, время, цена - требуют уточнения ⚠️

### Проблема с селекторами

Wizzair использует динамическую загрузку - ваши селекторы со страницы возможно **меняются** после выбора конкретного рейса.

### Как найти правильные селекторы:

1. Откройте сайт Wizzair
2. Выберите ваш рейс EVN → MIL на 8 марта
3. **ВАЖНО:** Кликните на конкретное время вылета
4. Дождитесь загрузки цен справа
5. Нажмите F12 → Найдите элемент с ценой
6. ПКМ → Copy → Copy selector

### Упрощённые селекторы (попробуйте эти):

```bash
# Если текущие не работают, замените в .env на:

# Попробуйте более простые селекторы:
PRICE_SELECTOR=.fare-button__price .price__value
DATE_SELECTOR=.flight-select__date
DEPARTURE_CITY_SELECTOR=.flight-select__departure .station
ARRIVAL_CITY_SELECTOR=.flight-select__arrival .station  
DEPARTURE_TIME_SELECTOR=.flight-info__departure time
ARRIVAL_TIME_SELECTOR=.flight-info__arrival time
```

## 🎯 Что будет происходить:

1. Каждые 30 минут парсер проверяет цену
2. При изменении - отправляет сообщение в Telegram
3. Данные сохраняются в `data/prices.json`

## 📝 Пример сообщения:

```
✈️ Wizzair - Обновление цены

📅 Дата: 08.03.2026
🛫 Вылет: Yerevan в 10:30
🛬 Прилёт: Milan в 13:45

📉 Цена снизилась!
Было: 89.99 EUR
Стало: 79.99 EUR
```

## 🐛 Если что-то не работает:

### 1. Селекторы не находят элементы:

Обновите их вручную (инструкция выше)

### 2. Docker не запускается:

```bash
# Проверьте что Docker запущен
docker ps

# Если нет - запустите Docker Desktop
open -a Docker
```

### 3. Бот не отправляет сообщения:

- Проверьте что начали диалог с ботом
- Проверьте Chat ID в `.env`
- Проверьте токен бота

### 4. Тестирование без Docker (локально):

```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Запустите тест
python test_selenium.py

# Запустите основное приложение
python -m src.main
```

## 📂 Структура данных

Данные сохраняются в `data/prices.json`:

```json
{
  "date": "08.03.2026",
  "departure_city": "Yerevan",
  "arrival_city": "Milan",
  "departure_time": "10:30",
  "arrival_time": "13:45",
  "price": "89.99",
  "parsed_at": "2026-01-08T14:53:39"
}
```

## 🔄 Изменение интервала проверки

В `.env`:
```bash
CHECK_INTERVAL=15  # Проверять каждые 15 минут
```

## ✨ Готово!

Запустите Docker и наслаждайтесь автоматическим мониторингом цен! 🎉
