# 🚀 Деплой на другой компьютер

## 📋 Предварительные требования

На целевой машине должно быть установлено:
- Docker
- Docker Compose
- Git

## 🔧 Инструкция по установке

### 1. Клонируйте репозиторий

```bash
git clone <ваш-репозиторий-url>
cd wizz_air_parcer
```

### 2. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
nano .env  # или vim, code и т.д.
```

**Обязательно заполните:**
```env
TELEGRAM_BOT_TOKEN=8481068655:AAGg... # Ваш токен от @BotFather
TELEGRAM_CHAT_ID=304497817            # Ваш chat ID от @userinfobot
FLIGHT_DATE=2026-02-08                # Дата вылета
DEPARTURE_CITY=EVN                    # Город вылета
ARRIVAL_CITY=MIL                      # Город прилёта
CHECK_INTERVAL=30                     # Интервал проверки (минуты)
```

### 3. Соберите и запустите Docker контейнер

```bash
# Сборка образа
docker-compose build

# Запуск в фоновом режиме
docker-compose up -d
```

### 4. Проверьте работу

```bash
# Просмотр логов в реальном времени
docker-compose logs -f

# Проверка статуса
docker-compose ps
```

Вы должны увидеть логи:
```
[timestamp] INFO - 🚀 Запуск Wizzair Screenshot Monitor
[timestamp] INFO - Маршрут: EVN → MIL
[timestamp] INFO - ✅ Скриншот успешно сохранён
[timestamp] INFO - ✅ Планировщик запущен
```

## 📱 Проверка Telegram

1. Откройте Telegram
2. Найдите вашего бота (например @wizz_air_parcerbot)
3. **ВАЖНО:** Если не делали раньше - нажмите `/start`
4. Дождитесь первого скриншота (~30-60 секунд)

## 🛠️ Управление контейнером

### Остановка
```bash
docker-compose stop
```

### Перезапуск
```bash
docker-compose restart
```

### Полное удаление
```bash
docker-compose down
```

### Обновление после изменений в коде
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### Просмотр логов за последние N строк
```bash
docker-compose logs --tail=50 -f
```

## 🐛 Устранение проблем

### Бот не отправляет сообщения
1. Проверьте что нажали `/start` у бота в Telegram
2. Убедитесь что `TELEGRAM_CHAT_ID` правильный
3. Проверьте логи: `docker-compose logs -f`

### Контейнер не запускается
```bash
# Проверка логов при запуске
docker-compose up

# Проверка ошибок сборки
docker-compose build
```

### Нет скриншотов / пустые скриншоты
- Это нормально на первых запусках
- Wizzair может показывать CAPTCHA - работаем с видимым браузером через Xvfb
- Интервал 30+ минут помогает избежать блокировок

### Зайти внутрь контейнера
```bash
docker-compose exec app bash
```

## 📊 Мониторинг

### Проверка использования ресурсов
```bash
docker stats
```

### Проверка размера логов
```bash
du -sh /var/lib/docker/containers/*/
```

### Очистка старых образов
```bash
docker system prune -a
```

## 🔄 Автозапуск при перезагрузке

Docker Compose уже настроен с `restart: unless-stopped` - контейнер автоматически запустится после перезагрузки системы.

Если нужно отключить:
```bash
docker-compose stop
docker update --restart=no <container_id>
```

## ⚠️ Важные замечания

1. **Видимый браузер**: Приложение использует Xvfb (виртуальный дисплей) для работы Chrome в видимом режиме
2. **Интервал проверки**: Рекомендуется ≥30 минут во избежание блокировки IP
3. **Скриншоты**: Сохраняются внутри контейнера в `/app/data/screenshots/`
4. **Логи**: Ротация логов настраивается через Docker (по умолчанию безлимитно)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь что `.env` правильно заполнен
3. Проверьте что начали диалог с ботом в Telegram

---

**Удачного мониторинга! ✈️**
