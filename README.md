# 🛫 Wizzair Screenshot Monitor

Автоматический мониторинг цен на авиабилеты Wizzair со скриншотами в Telegram.

## 📋 Описание

Приложение автоматически делает скриншоты страницы с выбранным рейсом Wizzair и отправляет их в Telegram через заданные интервалы времени.

**Почему скриншоты, а не парсинг?**
- ✅ Надёжно - не зависит от изменений структуры сайта
- ✅ Просто - не нужно искать CSS селекторы
- ✅ Наглядно - видите страницу так же, как в браузере
- ✅ Быстро - легко разобраться в ценах визуально

## 🚀 Быстрый старт

### 1. Настройка Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен бота
4. **ВАЖНО:** Найдите вашего бота в Telegram и нажмите **Start** (начать диалог)
5. Найдите [@userinfobot](https://t.me/userinfobot) и получите ваш Chat ID

### 2. Конфигурация

Файл `.env` уже настроен, проверьте параметры:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
WIZZAIR_URL=https://www.wizzair.com/.../2026-03-08/...
FLIGHT_DATE=2026-03-08
DEPARTURE_CITY=EVN
ARRIVAL_CITY=MIL
CHECK_INTERVAL=30
```

### 3. Запуск

#### Через Docker (рекомендуется)

```bash
# Запустите Docker Desktop
open -a Docker

# Соберите и запустите
docker-compose up --build -d

# Просмотр логов
docker-compose logs -f
```

#### Локально (для тестирования)

```bash
# Тест скриншота
python test_screenshot.py

# Запуск приложения
python -m src.main
```

## 📸 Как это работает

1. **Каждые N минут** (по умолчанию 30):
   - Открывается страница Wizzair с вашим рейсом
   - Ожидается загрузка (15 секунд)
   - Делается скриншот всей страницы
   - Скриншот отправляется вам в Telegram

2. **Скриншоты сохраняются** в `data/screenshots/` с timestamp'ом

3. **В Telegram приходит:**
   - Скриншот страницы с ценами
   - Информация о маршруте
   - Дата рейса
   - Время проверки

## 📊 Формат уведомлений

```
✈️ Wizzair - EVN → MIL

📅 Дата рейса: 2026-03-08
🕐 Проверено: 2026-01-08 15:05:14

[СКРИНШОТ СТРАНИЦЫ]
```

## ⚙️ Настройки

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Да |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | Да |
| `WIZZAIR_URL` | URL страницы с рейсом | Да |
| `CHECK_INTERVAL` | Интервал проверки (минуты) | Нет (по умолчанию 30) |
| `PRICE_SELECTOR` | CSS селектор цены | Нет |
| `DATE_SELECTOR` | CSS селектор даты | Нет |
| `DEPARTURE_CITY_SELECTOR` | CSS селектор города вылета | Нет |
| `ARRIVAL_CITY_SELECTOR` | CSS селектор города прилёта | Нет |
| `DEPARTURE_TIME_SELECTOR` | CSS селектор времени вылета | Нет |
| `ARRIVAL_TIME_SELECTOR` | CSS селектор времени прилёта | Нет |

## 📊 Формат уведомлений

```
✈️ Wizzair - Обновление цены

📅 Дата: 15.01.2026
🛫 Вылет: Warsaw в 10:30
🛬 Прилёт: Barcelona в 13:45

📉 Цена снизилась!
Было: 89.99 EUR
Стало: 79.99 EUR
```

## 🔧 Разработка

### Тестирование локально

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить
python -m src.main
```

## 📝 Логи

При запуске через Docker логи доступны:

```bash
docker-compose logs -f
```

## ⚠️ Важно

- Не коммитьте `.env` файл с реальными токенами
- Убедитесь, что CSS селекторы актуальны для текущей версии сайта
- Рекомендуемый интервал проверки: 15-30 минут

## 🐛 Troubleshooting

### Не приходят уведомления

1. Проверьте токен бота
2. Убедитесь что начали диалог с ботом
3. Проверьте Chat ID

### Не парсятся данные

1. Проверьте CSS селекторы через DevTools
2. Убедитесь что сайт доступен
3. Проверьте логи

### Docker не запускается

```bash
# Пересоберите контейнер
docker-compose up --build
```

## 📜 Лицензия

MIT

## 👤 Автор

Личный проект для мониторинга цен Wizzair
